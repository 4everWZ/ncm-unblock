Place the official UnblockNeteaseMusic Windows x64 standalone here as:

  UnblockNeteaseMusic.exe

Pinned release: UnblockNeteaseMusic/server v0.28.0
Asset: unblockneteasemusic-win-x64.exe (rename to UnblockNeteaseMusic.exe)

BetterNCM only loads `UnblockLite.plugin` (a zip). After the plugin is
installed, preferred UNM placement is:

  <BetterNCM data path>/UnblockLite/UnblockNeteaseMusic.exe

You may also place it next to the extracted plugin files under
`plugins_runtime/UnblockLite/core/`.

This package does not redistribute that binary. The upstream standalone
contains its own Node runtime; no separate Node install is required.
